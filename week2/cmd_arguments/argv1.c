#include <stdio.h>
#include <cs50.h>

int main(int argc, string argv[])
{
    int total = 0;
    for (int i = 1; i <= argc; i++)
    {
        total = i;
    }

    //printing number of all arguments:
    printf("Total Arguments:> %i\n", total);
}